# AGENT.md

The agent working agreement for this repo lives in **[AGENTS.md](./AGENTS.md)** —
that's the filename Claude Code and other agents auto-load each session (a
singular `AGENT.md` is not picked up automatically), so the content is kept
there to stay in force. This file is just a signpost.

See also `CLAUDE.md` (what the app is + its constitution) and `BACKLOG.md`
(the tracked request list).
